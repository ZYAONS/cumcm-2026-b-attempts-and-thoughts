# -*- coding: utf-8 -*-
"""selfcheck_time.py -- record the time-compression round."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 32 | "方向一（搜索与清除解耦）"的两种实现**都是负优化** | 批量重规划 10499→11061 s；分阶段 10499→12034 s。真因：**清除会移除认证负担**，延后清除反而让扫描更长——两阶段是通过"认证"耦合的，不只是通过"路径" | 均撤销并记录；§10.2 第 2 条据此改写 |
| 33 | 18 个额外站位在**认证"不可能被认证"的候选点** | 边界候选点用域内读数不可认证（定理 2）；已被听到的频道根本不需要"不存在的证明" | 认证集合限定为从未听到的频道；边界候选点按 `verify_margin` 排除 |
| 34 | 关掉认证搜索后**认证再也没被评估**，每站都测全部 20 个频道 | 认证原本只在 `next_search_stop` 里评估 | 加入扫描中的**周期性认证**，已认证频道立刻停测 |
| 35 | 截断扫描站位时按半径排序等于**砍掉最外圈** | 最外圈是边界附近源的唯一机会 | 改为**最远点采样**排序，截断后仍保持覆盖 |
| 36 | 调参池上得到的 "498.1 s" **在独立池上不复现** | 调参池（30000+，20 例）偏易 | 弃用该数字；在独立池（62000+/51000+，各 40 例）上重新寻优，最终采用 `s1000/cap10` |

### 3.7 把问题四压到 500 s 以内（独立池验证）

| 配置 | 池 A（62000+，40 例） | 池 B（51000+，40 例） |
|---|---|---|
| 论文当前配置 | 864.9 s / 1.0000 | 945.7 s / 0.9983 |
| **s1000 cap10（快速模式）** | **476.5 s / 0.9823**（中位 475.7） | **498.2 s / 0.9699**（中位 485.0） |
| s1100 cap9 | 414.8 s / 0.9454 | 429.8 s / 0.9395 |

\textbf{结论：问题四可以做到 500 s 以内，且在两个独立算例池上复核通过}，
代价是完成率下降 1.6~3.0 个百分点（时间下降 45~47%）。
配置已固化为 `make_data.P4_FAST`，默认仍使用精度优先的 `P4`；
快速模式可用 `Q4_FAST=1 python run_http_tests.py q4 ...` 触发。
完整报告见 `docs/time_optimization.pdf`（含 12 项尝试，其中 5 项为负结果或无效）。

**路径已无余量**：实测探测行程（12642 m）甚至优于同样站位的最优 2-opt 巡回（15880 m）。
剩余三条压缩方向（减少每站检测频道数、顺路清除、提高发现效率）在该报告第 7 节量化。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
