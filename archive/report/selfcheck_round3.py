# -*- coding: utf-8 -*-
"""selfcheck_round3.py -- record this round (relocate / exhaustive / negatives)."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 37 | relocate 的**状态机写在"生成任务"里**，每次重规划就推进一格 | `_build_tasks` 每个动作后都调用，于是状态在**没有任何候选点被访问**的情况下跑完，6 个优质点位全被丢弃 | 查询与推进分离：`_relocate_point` 只读状态，`_relocate_advance` 在执行后推进 |
| 38 | 末段图案半径 0.55σ 时，图案空隙可能整片落在 20 m 清除半径外 | 实测 seed 12010：已定位到 σ=42 m 却 14 次清除全失败、硬上限用尽 | 新增**栅格穷举兜底**（间距 15 m，15/√2=10.6 m < 20 m，必然命中） |
| 39 | 顺路清除实测**负优化** | 关闭 1.0000/540.3 s；开半径 520 m 后 0.9946/573.3 s。清除自身的归航与图案代价超过省下的绕行 | 已撤销并记录（与上一轮"分阶段"失败同源） |
| 40 | relocate 偏移"由小到大"**丢完成率** | 局部行程减半（2529→1191 m）但总行程反增、最差完成率下降 | 恢复"由大到小" |
| 41 | 单一算例池选型**双向误导**（第三次同类陷阱） | 调参池上 s900 最好（1.0000/540.3），池 A 上却是 s1000 最好（1.0000/539.3），两池结论相反 | 改为**四池联合、以最差池为准** |

### 3.8 三条攻坚方向的最终结果（四池 120 例，约 1560 个源）

| 方向 | 结果 | 判定 |
|---|---|---|
| **贴边定向源** | relocate（横向偏移二分 + 沿射线爬行）+ 末段栅格穷举清除。**漏源 3 → 1**，平均时间 885.3 → **880.7 s** | **有效** |
| **减少每站检测频道数** | 实测每站由 20 降到 9~16（随频道解算递减）；新增"附近已全部认证则不测"规则逻辑严格但**几乎不触发**（405 次调用 0 次跳过）——认证已近信息下界 | 部分完成 |
| **顺路清除 + 混合** | 顺路清除**负优化**；混合的真正答案被找到：**认证搜索兼任定向源发现器**（P4 的 17 个补站放在"证书最弱"处，那正是定向源藏身处） | 认知突破，实现上不采用顺路清除 |
| **完成度完整下压缩时间** | **未同时达成**。约 880 s ↔ 最差 0.9978（1/120 失败）；约 534 s ↔ 最差 0.9833（14/120 失败） | 真实前沿，如实报告 |

仅剩的 1 例漏源经几何核算属**问题固有极限**：源在 $(1767,-219)$、辐射方向与朝外径向
夹角余弦 0.996，**域内受光区仅 0.011 km²（占全域 0.11%），是一条 27 m 宽的月牙**；
要覆盖它需在 11.3 km 边界上每 25 m 布站（约 450 站），不可行。
故本文把可达完成率上界记作 **1 − 0.064% = 0.9994**。

完整报告见 `docs/smart_algorithms.pdf`（含 12 项尝试的判定表）。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
