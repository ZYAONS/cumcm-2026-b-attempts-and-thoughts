# -*- coding: utf-8 -*-
"""selfcheck_round6.py -- 记录本轮：两个实现 bug、负结果、以及"实测=下界"的结论。"""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 52 | 让贴边环在 ring 模式下生效时**引入回归**：s950 rim12 从 24.5 站掉到 15.1 站、完成率 1.0000→0.9910 | 改为局部变量 `base` 后，`_rim_done` 已置位，**第二次调用不再追加贴边环**（25 站变 13 站） | 写回 `self.survey_stops` 修正 |
| 53 | 写回实例属性后 **ring 模式仍不生效**（rim0 与 rim12 结果逐位相同） | `base` 由 `_make_ring_stops()` 每次重新生成，基础环把贴边环"盖掉" | 改为**一次计算、永久缓存**（`_primary_cache`） |
| 54 | 减少预规划站位会**触发昂贵自适应补站** | cap6：站位 11.8（更少）但行程 27612 m（更多）、6/40 失败。自适应补站是贪心的，路线差得多 | 判定：**预规划的一圈胜过步步最优的贪心**（第三次得到同一结论） |
| 55 | 归航走得更远反而更差 | 步长系数 0.7→1.0 使清除行程 9467→11440 m；过冲后要回退 | 保持 0.7 |
| 56 | 贴边环减到 6/8 个点：2 池上 0/40 失败，4 池上 5/120 失败 | 又一次被 2 池误导（本项目第五次） | 保留 rim12 |

### 3.11 本轮结论：**实测时间已等于"行程 + 检测"的下界**

行程审计（10 例，本配置）：

| 任务 | 腿数 | 行程/m | 占比 | 折算 |
|---|---|---|---|---|
| 探测（普查） | 22.8 | 13246 | 60.7% | 212 s/源 |
| 清除 | 26.0 | 6233 | 28.6% | 100 s/源 |
| 补测 | 5.9 | 2336 | 10.7% | 37 s/源 |
| **合计** | | **21815** | | **349 s/源** |
| 检测 424 次 | | | | 202 s/源 |
| **总计** | | | | **551 s/源** |

**实测 570~596 s/源 ≈ 该分解，几乎没有浪费。**

而三个方向都被实测堵死：
1. **减站位** → 触发自适应补站，行程反增（cap6：站位 −37%，行程 +20%）；且实测路径
   与同点集 2-opt 巡回只差 8%——**路由无余量**；
2. **减检测** → 8 组参数下稳定在 420~440 次；
3. **减归航腿** → 步长加大反而更差。

**最终交付配置：595.7 s/源 @ 最差完成率 0.9978（120 例 1 例失败）**，
相比本轮开始时的 880.7 s **下降 32%**，完成率逐例相同。
**300~400 s 在当前问题设定下不可达**，证据见 `docs/why_not_300.pdf`。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
