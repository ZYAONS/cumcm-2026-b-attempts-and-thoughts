# -*- coding: utf-8 -*-
"""selfcheck_corr.py -- record the correlation study and the residual failure mode."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 30 | 误差相关性实验**设计不公平**：把"50% 相关"误判为最差档 | 独立误差 $U(-1,1)$ 的 sd 是 0.577，而正弦叠加场的 sd 只有 0.419 —— "更相关"其实是"更容易"，50% 档夹在中间 | 把平滑场按标准差归一化（×1.378）后重做，见 3.5 |
| 31 | 短基线横向偏移尝试造成**完成率下降 4 倍**（漏源 2/914 → 8/914） | 放宽后的预算条件让"已有两条方位"的频道无限申请站位，挤占普查覆盖预算却未收紧 σ；单频道出现 5974 次 no_signal | **已撤销**；参数保留并标注"实测负优化、未使用" |

### 3.5 误差相关性的公平对照与残留失败模式

**公平对照**（平滑场按标准差归一化，三个池子共 914 个源）：

| 相关比例 | 误差 sd | 漏源/总数 | 完成率 |
|---|---|---|---|
| 0%（当前实现） | 0.577 | 2~3/914 | 0.9969~0.9979 |
| 50% | 0.407 | 2~4/914 | 0.9951~0.9979 |
| 100% | 0.551 | 4/914 | 0.9952 |

结论：**"50% 最差"是噪声**（0% 同样漏 2~3 个）；相关性带来约 2 倍的漏源率（0.22%→0.44%），
机制是**共享偏差在求交时不抵消**；但本文的估计器用**硬约束交集**，结论与相关性无关。

**顺带暴露的真正残留失败模式**：即使误差完全独立，仍有 **0.2~0.35%** 的源被漏掉。
这些算例结构完全相同——**贴边（r≈1720~1760 m）、定向、唯一一条方位取自目标域边界**。
机器人当时离源仅 85 m 且在受光侧，却因"沿射线 550 m 的站位越过源落到背光侧"而永久停留。
两次改进尝试：沿射线小步**爬行**（观测数 1→2，整体**中性**，保留）、
**短基线横向偏移**（**负优化**，已撤销）。

### 3.6 时间预算与剩余优化方向

问题四（16 例均值）：**移动占 75.2%**、检测 20.1%、切换 3.8%、清除脉冲 0.9%。
按任务类：普查站位占 67.6% 时间与 73.6% 行程。25.5 个搜索站位的最近邻巡回只需
18981 m，而实际搜索行程 29442 m —— **多出 10.5 km（约 20% 时间）**。

| 方向 | 潜在收益 | 说明 |
|---|---|---|
| 搜索与清除**分阶段**调度 | ~15~20% 时间 | 结构性改动，需重写任务调度 |
| 按站位**选择检测频道** | ~10% 时间 | 429 次检测可压到约 250 次 |
| 主动**重捕获**策略 | 完成率 0.2~0.35% | 针对贴边定向源，与现有框架并行 |
| 参数再调 | 已到平台 | 8~9 个参数在窄带内无显著差异 |

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
