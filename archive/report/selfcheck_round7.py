# -*- coding: utf-8 -*-
"""selfcheck_round7.py -- 记录"用新想法冲刺 300-400 s"这一轮的四个新机制与结果。"""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 57 | **新机制一：分段移动 + 途中测量**。发现 `move_measure` 是"一步跳到目标"，路过区域从未采样 | 途中测量确实产生信息（检测 452→618），但**普查站位一个都没少**，净增 76 s | 判定无效；根因是"认证是全有或全无" |
| 58 | **新机制二：站位级跳过**（邻域已完全认证则不去） | 24.1 站位一个都没跳过——认证需要周围 3 个站位都到访才成立，前期任何邻域都不完整 | 判定无效（逻辑正确但不触发） |
| 59 | **新机制三：7 点环保底发现**（覆盖半径 $\le R_{\min}$） | ring1155 rim0：0.9673（6/16 失败）；加 rim12 后 1.0000 但 627.9 s（比格点方案慢） | 判定：**格点方案更优** |
| 60 | **发现 `max_survey_stops` 的语义错误**：它清空**全部**待访站位，贴边环排在格点之后被一起砍掉 | 这解释了此前所有 cap 实验的异常（cap8 时贴边环 12 个点一个都没去） | 修正为**只限制格点、贴边环永远保留** |
| 61 | 修正后重测"少布格点 + 保留贴边环" | 站位 24.1→14.6（−39%）但**行程 24010→27513 m（+15%）**、2/40 失败 | 判定负优化；**第四次确认：削减预规划站位会让自适应补站接管，而它路线更差** |

### 3.13 结论：四个新机制全部为负，595.9 s 是当前最优

本轮针对 300~400 s 的目标设计了四个**此前不存在**的新机制，逐一实测：

| 新机制 | 结果 |
|---|---|
| 分段移动 + 途中测量 | 净增 76 s，无效 |
| 站位级跳过（邻域认证完则不去） | 从不触发，无效 |
| 7 点环保底发现 | 0.9673 或 627.9 s，均劣于格点方案 |
| 只限制格点、保留贴边环 | 站位 −39% 但行程 +15%，负优化 |

**四池最终验证（120 例）**：

| 配置 | 最差完成率 | 平均时间/s | 失败算例 |
|---|---|---|---|
| **rim12（现行）** | **0.9978** | **595.9** | **1/120** |
| rim6 | 0.9947 | 576.8 | 5/120 |
| rim8 | 0.9950 | 585.6 | 5/120 |
| rim4 | 0.9856 | 582.6 | 11/120 |

**595.9 s / 0.9978 是当前架构的最优点**：实测时间 = 行程（349 s/源）+ 检测（202 s/源）
+ 清除脉冲，**几乎没有浪费**；而行程已实测处于同点集 2-opt 巡回的 8% 以内。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
