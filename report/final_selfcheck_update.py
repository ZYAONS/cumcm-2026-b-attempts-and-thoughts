# -*- coding: utf-8 -*-
"""final_selfcheck_update.py -- record this round in the self-check report."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

s = s.replace("| 6 | 是否记录每个错误与解决方案 | ✅ | 见第 3 节（共记录 24 个真实故障，",
              "| 6 | 是否记录每个错误与解决方案 | ✅ | 见第 3 节（共记录 28 个真实故障，", 1)

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 25 | **归航沿"最后一条方位"前进，但那条方位是在别处测的** —— 机器人朝反方向走，来回振荡（案例 910001：已把源定位到 4.2 m，却在 400 m 外打转，1733 s 未清除） | 方位角只在测量它的那个点有意义；从当前位置复用该角度偏差达 58°，步长减半规则把它变成极限环 | 归航步改为**朝估计点前进**；当机器人确实站在观测点时两者重合，无损失。该案例 ratio 0.909→1.000、时间 −35% |
| 26 | 末段图案扫完把机器人留在**最后一个外圈点**上（可能是定向源的背光侧） | 图案顺序为"先中心后外圈" | 改为**先外圈、中心最后**，失败后停在估计点；代价为零 |
| 27 | 末段三次观测点**近乎共线**，σ 停在 77 m（真值只差 20.5 m，恰在 20 m 半径外 0.5 m） | 沿直线归航导致共线观测，共线的方位无法约束距离 | 新增**垂向截断测量**：垂直视线补一次观测，σ 77 m→1 m |
| 28 | 贴边朝外的定向源，第二站位 ±55° 偏移**落到背光侧** | 这类源的域内受光区是薄月牙 | 站位偏移角随失败次数收缩（45°→20°→0°），最终回到射线上（射线逼近引理保证受光） |
| 29 | 正式测试与演练用的参数集**不是同一套**（`run_http_tests` 自建参数，漏掉调优值） | 参数在两处各写一份 | `run_http_tests` 改为直接 import `make_data.P3/P4`，单一来源 |

### 3.3 §10.2 四条改进方向的实测结论（本轮）

| 条目 | 处理 | 结论 |
|---|---|---|
| (1) 完成率损失约 4% | 诊断出真实原因是**末段定位精度**与**站位落在背光侧**，而非"域内不可认证" | **已解决**：独立池完成率 0.9981→**1.0000**（最差 0.9231→1.0000），时间 −36.7% |
| (2) 路径规划的耦合 | 实现"顺路清除"与"自适应补测间隔"两个具体耦合 | 顺路清除**无效**（优先权偏置已隐含实现）；自适应补测 **−3.3% 时间**，已启用 |
| (3) 误差空间相关性 | 注入波长 1200~3000 m 的相关随机场，相关比例 0/50/80/100% | **无需建模**：差异在运行波动内；原因是估计器用**硬约束交集**，结论与误差是否相关无关 |
| (4) 真实时间耦合 | 实测 799 次请求共 5.18 s（6.48 ms/请求），余量 **232×** | **本地环境无需建模**；官方 GUI 响应可能慢得多，需在该环境复测 |

完整记录见 `docs/optional_changes.pdf`（12 项尝试：4 项有效、6 项负优化、2 项无效）。

### 3.4 最终指标（独立算例池 + 正式测试）

| | 完成率 | 最差完成率 | 平均时间/s | 平均行程/m |
|---|---|---|---|---|
| 问题三 优化前 | 1.0000 | 1.0000 | 326.3 | 13490 |
| 问题三 优化后 | 1.0000 | 1.0000 | 325.8 | 13448 |
| 问题四 优化前 | 0.9981 | 0.9231 | 1377.6 | 67501 |
| 问题四 优化后 | **1.0000** | **1.0000** | **871.3** | **41714** |

演练池：问题三 60 例 **1.0000 / 326.8 s**；问题四 60 例混合 **1.0000 / 888.3 s**、
30 例全定向 **1.0000 / 983.4 s**、30 例全全向 1.0000 / 860.6 s；
问题四灵敏度 8 组扰动**全部 1.000**。
正式测试：问题三 15/15、12/12、10/10；问题四 **11/11、12/12、11/11（三局全部清除）**。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
