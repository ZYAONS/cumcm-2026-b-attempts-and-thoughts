# -*- coding: utf-8 -*-
"""selfcheck_round4.py -- record the 32 % speed-up round."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "self_check.md")
s = io.open(p, encoding="utf-8").read()

anchor = "## 4. 关键数值的合理性判据"
new = r"""| 42 | 贴边环取 **r=1750** 时无法发现 $r_q>1750$ 且朝外的源 | 受光半平面要求 $(p-q)\cdot\hat u\ge0$；朝外时即 $|p|\ge r_q$，观测点必须比源更靠外 | 记录为几何约束；实测把环推到 1780/1795 **反而更差**（贴边环点几何劣化），故保持 1750 |
| 43 | **自适应采样**（取消固定格点，由认证贪心驱动）实测**慢一倍** | batch1：1003 s、行程 46610 m；batch4：694 s、30572 m。原因是贪心补站没有全局视野，一批一批加导致路线反复穿越全域 | 判定负优化；结论：**"预先规划的一圈"胜过"步步最优的贪心"** |
| 44 | 环组布局认证**最多只到 88.6%** | 环上点角度太稀疏，两环之间的候选点读数集中在窄角度内、无法环绕 | 判定无效；结论：**三角格点是认证的正确结构** |
| 45 | 贪心集合覆盖**无法启动**（单站边际增益恒为 0） | 认证一个候选点需要至少 3 个环绕读数 | 判定无效；结论：**认证不是覆盖问题而是环绕问题** |
| 46 | 提高 `probe_min_angle` 22→70、限制每站频道数 | 612.3 s（噪声内）／679.8 s（更差） | 无效或负优化，已记录 |

### 3.9 本轮结果：完成率不变，时间 −32%

**关键改动**：贴边环 12 点 @ r=1750 m + 认证边距 700 m + 格点间距 950 m。

| 四池 120 例（按最差池） | 最差完成率 | 平均时间/s | 失败算例 |
|---|---|---|---|
| 优化前 | 0.9978 | 880.7 | 1/120 |
| **优化后** | **0.9978** | **597.0** | **1/120** |

正式测试：问题四三局 **14/14、12/12、14/14 全部清除**，平均 **579.2 s**（此前 1090.6 s，−47%）。

**下界核算**：13 个源的 2-opt 巡回 8635 m=1727 s，加普查/检测/清除动作共 2447 s=**188 s/源**；
但定向源的认证要求格点间距 ≤958 m（三向环绕），全域需 **13 个站位**，
其位置与源无关、行程无法与清除共用，计入后现实下界约 **350 s/源**。
**因此在保持 0.99+ 的前提下，200~300 s 不可达**；本轮 597 s 距该下界约 1.7 倍。

本轮共 14 项尝试，其中 **9 项为负结果或无效**，全部记录在案。
完整报告见 `docs/four_minutes.pdf`。

## 4. 关键数值的合理性判据"""
assert anchor in s, "anchor missing"
s = s.replace(anchor, new, 1)
io.open(p, "w", encoding="utf-8").write(s)
print("self-check updated")
