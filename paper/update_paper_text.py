# -*- coding: utf-8 -*-
"""update_paper_text.py -- the three narrative sentences that quote the old
problem 4 numbers."""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
p = os.path.join(HERE, "paper_p3.tex")
s = io.open(p, encoding="utf-8").read()

pairs = [
    ("三次正式测试共清除 33 个干扰源中的 33 个已发现目标（第 2 局 12 个源中清除 11 个，完成率 0.917），平均定位清除时间 602.6 s、654.5 s、718.8 s（均值 658.6 s），程序运行时间均小于 2.5 s。第 2 局未清除的干扰源是一个位于目标域边缘、定向方向朝外的源，属于第 8.4 节讨论的“半平面黑洞”极端情形；开启域外补测机制后该类情形可被清除，但时间代价较大。",
     "三次正式测试共清除 34 个干扰源中的 34 个（三局完成率均为 100\\%），平均定位清除时间 966.4 s、920.8 s、1204.3 s（均值 1030.5 s），程序运行时间均小于 5 s。此前版本在第 2 局漏掉了一个位于目标域边缘、定向方向朝外的源（属于第 8.4 节讨论的“半平面黑洞”极端情形）；在修正认证扫描的完整性缺陷并重新调参后，该类情形也能被清除——第 8.6 节给出了这一修正的完整证据链。"),
    ("可见问题四的完成率对模型参数不敏感（0.952$\\sim$0.979），其损失主要来自少数几何上无法认证的极端布局（贴边且朝外的定向源），而不是算法对参数的敏感性。",
     "可见问题四的完成率对模型参数不敏感（0.989$\\sim$1.000），其损失主要来自少数几何上难以认证的极端布局（贴边且朝外的定向源），而不是算法对参数的敏感性。"),
    ("要点：(i) 完成率对全部扰动均稳健（问题三恒为 1.000，问题四在 0.952$\\sim$0.979）；",
     "要点：(i) 完成率对全部扰动均稳健（问题三恒为 1.000，问题四在 0.989$\\sim$1.000）；"),
    ("（后者体现为不同算例的标准差 46.6 s 与 260.0 s）",
     "（后者体现为不同算例的标准差 61.0 s 与 238.6 s）"),
]
for a, b in pairs:
    if a not in s:
        print("  !! MISSING:", a[:56])
        continue
    s = s.replace(a, b, 1)
    print("  ok:", a[:56])
io.open(p, "w", encoding="utf-8").write(s)
print("done")
